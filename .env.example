# Set the real public origin before publishing. Without it, indexing is disabled.
SITE_URL=
# Set true on staging/preview deployments, even if SITE_URL is present.
PREVIEW_MODE=false
# Project Pages only. Leave empty for local development.
NEXT_PUBLIC_BASE_PATH=
